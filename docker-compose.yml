services:
  autoscraper:
    build: .
    container_name: kbtu-attendance
    restart: unless-stopped
    shm_size: '256m'
    deploy:
      resources:
        limits:
          memory: 1g
    env_file:
      - .env
    volumes:
      - ./data:/app/data
